# Assets

Drop your application's static resources here — icons, images, fonts, and other
files your UI loads at runtime. This folder ships with the `cascade-app` template
so generated apps have a conventional home for resources.
